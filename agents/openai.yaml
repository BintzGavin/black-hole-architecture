interface:
  display_name: "Roll Out Black Hole Architecture"
  short_description: "Scaffold vision-driven planner and executor roles"
  default_prompt: "Set up Black Hole Architecture in this repository with a single Vision, disjoint roles, and committed planner/executor prompt pairs."
