interface:
  display_name: "Roll Out Black Hole Architecture"
  short_description: "Scaffold continuous planner and executor roles"
  default_prompt: "Use $roll-out-black-hole-architecture to set up this repository with one Vision, disjoint roles, role-local ledgers, and an hourly alternating planner/executor cadence."
